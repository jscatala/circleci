machine:
    timezone:
        America/Santiago

    environment:
        PROJ_FOLDER: "CicleCI_project"

    python:
        version: 3.6.0


dependencies:
    pre:
        - sudo apt-get update 
        - sudo apt-get upgrade
